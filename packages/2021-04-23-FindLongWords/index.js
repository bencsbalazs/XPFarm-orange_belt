export const longWords = (sentence, n) => {
    let filteredWords = []
    if (typeof(sentence) != "string") {
        return false
    }
    if (n<1) {
        return false
    }
    return filteredWords
}
